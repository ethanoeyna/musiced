# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Runtime state (config, queue)
data/

# Claude Code per-machine state
.claude/

# Spotipy / spotdl token caches (contain live OAuth tokens — never commit)
.cache
.spotdl/
